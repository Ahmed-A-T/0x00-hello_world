#include "main.h"

/** int _islower - checks for lowercase character
 *
 * Return: returns 1 if c is lowercase returns 0 otherwise
 *
 */
int _islower(int c)
{
	int ch = 65;

	for (ch = 65; ch <= 90; ch++)
	{
		if (c == ch)
			return (1;
		else
			return 0;
	}
}
