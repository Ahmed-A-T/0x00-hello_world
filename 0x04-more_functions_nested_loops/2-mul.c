#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply
 *
 * Return: returns the multiplication of the two numbers
*/
int mul(int a, int b)
{
	return (a * b);
}
